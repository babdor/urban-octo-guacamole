apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hello-world.fullname" . }}
  labels:
    {{- include "hello-world.labels" . | nindent 4 }}
data:
  index.html: |
    {{- .Files.Get "index.html" | nindent 4 }}
